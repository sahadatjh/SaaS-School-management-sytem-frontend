"use client";
import { useRouter } from "next/navigation"; import { useState } from "react"; import { platformApi } from "@/lib/platform-api";
export default function PlatformLogin() { const router = useRouter(); const [error, setError] = useState(""); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); const form = new FormData(e.currentTarget); try { await platformApi.login(String(form.get("email")), String(form.get("password"))); router.replace("/platform/institutions"); } catch (err) { setError(err instanceof Error ? err.message : "Unable to sign in."); } } return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="w-full max-w-sm space-y-4 rounded-xl bg-white p-6 shadow"><h1 className="text-xl font-bold">Super Admin</h1><input required name="email" type="email" placeholder="Email" className="w-full rounded border p-2"/><input required name="password" type="password" placeholder="Password" className="w-full rounded border p-2"/>{error && <p role="alert" className="text-sm text-red-600">{error}</p>}<button className="w-full rounded bg-orange-600 p-2 text-white">Sign in</button></form></main>; }
